Drop the Livewire logo PNGs here:

- logo-stacked-color.png  (grey wordmark + orange swoosh, for light backgrounds)
- logo-stacked-white.png  (white wordmark + orange swoosh, for the dark masthead)

The proposal generator embeds whichever exists as a data URI automatically
(logoFileDark wins on the dark masthead). Paths are configured in ../livewire.json.
